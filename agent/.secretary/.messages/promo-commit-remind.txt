커밋 완료됐어. 이번 작업에서 배운 게 있으면 promotion-log.md에 기록해줘.

파일: ~/.claude/memory/promotion-log.md
기록 방법: 전체 Read 불필요. 마지막 항목 확인은 grep으로만:
  grep "^### " ~/.claude/memory/promotion-log.md | tail -3

**K (새 발견/지식)** — API 동작, 예상 못한 제약, 유용한 도구 발견:
### YYYY-MM-DD · {키워드}
**유형**: K
**발견**: {새로 알게 된 것 — 다른 에이전트가 재현할 수 있도록 구체적으로}
**제약**: {적용 시 알아야 할 제한 조건} (없으면 생략)
**적용**: {어디에, 언제 쓸 수 있는지}
**원본**: {참고한 파일/문서 경로}
**source_project**: {현재 프로젝트 경로}

**P (패턴)** — 반복 코드/아키텍처/운영 패턴:
### YYYY-MM-DD · {키워드}
**유형**: P
**발견**: {어떤 패턴을 발견했는지 — 재현 가능하게}
**제약**: {적용 조건 / 함정} (없으면 생략)
**적용**: {어디에, 언제 쓸 수 있는지}
**원본**: {참고한 파일/문서 경로}
**source_project**: {현재 프로젝트 경로}

기록할 게 없으면 스킵해도 돼.
