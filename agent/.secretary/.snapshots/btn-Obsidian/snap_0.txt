  │               항목                │ 상태 │
  ├───────────────────────────────────┼──────┤
│ 세션 생성 순서 (생성→wt→스폰)     │ ✅   │
├───────────────────────────────────┼──────┤
  │ 기존 동명 세션 정리               │ ✅   │
  ├───────────────────────────────────┼──────┤
  │ -x 200 -y 50 옵션                 │ ✅   │
  ├───────────────────────────────────┼──────┤
  │ wt.exe 위치별 배치                │ ✅   │
  ├───────────────────────────────────┼──────┤
  │ strategic → opus, 나머지 → sonnet │ ✅   │
  ├───────────────────────────────────┼──────┤
  │ bypasspermission 폴링             │ ✅   │
  ├───────────────────────────────────┼──────┤
  │ Trust Enter                       │ ✅   │
  └───────────────────────────────────┴──────┘

  누락된 부분:

  1. --system-prompt 미포함 — spec은 역할별 고유 system-prompt를 정의함:
    - Worker: "Harness Worker. sessions: worker/{SUPERVISOR}/verifier/healer/strategic..."
    - Verifier: "...FAIL 판정 시 promotion-log에 ERROR 기록..."
    - Healer: "...수정 대기 중인 FAIL Sub-obj 복원..."
    - Strategic: "...--model opus... 리서치 원본 저장..."

  현재 script는 claude --dangerously-skip-permissions --model ${MODEL}만 실행하고 system-prompt 없음.
  2. 핸드셰이크 프로토콜 미구현 — spec은 ACK 파일 기반 핸드셰이크 정의 (.harness/acks.txt), script는 role injection만
   함.
  3. PSMUX_SESSION 전달 방식 — spec은 cmd.exe 환경변수 방식 OK이므로 이건 일치하지만, SUPERVISOR_SESSION 변수가
  system-prompt에 포함되어야 하는데 누락.

  스펙대로 맞추려면 spawn-session.sh에 system-prompt와 핸드셰이크를 추가해야 합니다. 진행할까요?

❯ 아. psmux 를 agent 가 sh 로 생성하면 handshake 까지 해주는걸로 할 수 있다고?

● Flibbertigibbeting…

─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯
─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
⏵⏵bypasspermissionson (shift+tabtocycle)·esctointerruptRemoteControlactive
